public interface IncreaseByFactor {
    //kinda similar to abstract class, but can have multiple interfaces instead of one subclass when "extending" (like inheritance)
    //similar to class, but is diff
    //use CAPITAL LETTERS

    int FACTOR = 5;
    void getNext();
    void setStart(int x);


}
