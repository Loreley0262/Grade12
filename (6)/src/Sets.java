import java.util.HashSet;
import java.util.Random;

public class Sets {
    /*tree set, hash set,
    no duplicates

    hash set doesn't let you add duplicate methods, returns false when you try to sout adding smth you already have
    treeset is different, still cant duplicate

    treeset
        head set, tail set, iterator
        tail set is less than or equal to
        head set is greater than

    */
    public static void main(String[] args) {
        Random rand = new Random();
        int age = rand.nextInt(100);
        System.out.println(age);
    }


}
